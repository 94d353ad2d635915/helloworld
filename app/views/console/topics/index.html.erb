<p id="notice"><%= notice %></p>

<h1>Topics</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= topic.title %></td>
        <td><%= topic.posttext.body if topic.posttext%></td>
        <td><%= topic.user %></td>
        <td><%= link_to 'Show', console_topic_path(topic) %></td>
        <td><%= link_to 'Edit', edit_console_topic_path(topic) %></td>
        <td><%= link_to 'Destroy', console_topic_path(topic), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Topic', new_console_topic_path %>
